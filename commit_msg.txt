feat: quitar geolocalización por IP (solo GPS + ciudades + manual)

Manu pidió quitar la detección por IP — siempre devolvía Alemania
(Frankfurt, IP del servidor Vercel en lugar de la del cliente), y
prefería un control más explícito.

Cambios en components/race-distance-filter.tsx:
- Eliminado useState requestingIp
- Eliminada función requestIpLocation (fetch a /api/geo/ip)
- Eliminado useEffect de auto-detect al cargar la página
- Eliminado botón "Usar mi IP" del JSX
- Eliminado estado detectedCountry / detectedCity
- Eliminado import Globe (ya no se usa)
- Eliminado import de useCallback que se usaba solo para requestIpLocation
- Eliminado label "🌐 IP (ciudad)" del indicador de fuente
- Texto del iframe ahora dice "elige una ciudad o introduce coordenadas"
  en lugar de "Usa IP o coordenadas manuales"
- Texto del modal de ayuda ahora dice "elige una ciudad o introduce
  coordenadas" en lugar de "Usa IP o coordenadas manuales"
- El slider de slider de distancia se mantiene sin cambios

Métodos disponibles ahora:
1. GPS del navegador (botón "Activar GPS" con permiso)
2. Selector de ciudades españolas (1 click, 14 ciudades)
3. Coordenadas manuales (lat/lng)

El sessionStorage ahora guarda también `source` y `sourceLabel` para
restaurar la fuente correcta al recargar. Si el source guardado era
"ip" (versión vieja), se migra a "preset" automáticamente.

Cambios en app/api/geo/ip/route.ts:
- Reemplazado el handler real por un 410 Gone con mensaje explicativo.
  El endpoint ya no se llama desde ningún sitio; mantener el archivo
  evita imports rotos si algún cache tiene la ruta vieja.
