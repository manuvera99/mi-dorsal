feat: página de diagnóstico de geolocalización (/test-geo)

Manu reporta que el botón "Activar GPS" no muestra el prompt nativo del
navegador. La causa más probable es una extensión (uBlock, Privacy Badger,
Brave Shields) bloqueando navigator.geolocation, pero necesitamos
herramientas para diagnosticarlo en el navegador real, no a ciegas.

Cambios:

1. Nueva página /test-geo (pública, sin auth) que ejecuta 8 checks sobre
   la API de geolocalización y muestra el resultado exacto:
   - navigator existe
   - secure context (HTTPS)
   - no está en iframe
   - navigator.geolocation existe
   - Permissions API state (granted/prompt/denied)
   - navegador detectado (Chrome/Edge/Firefox/Safari/Brave/Opera)
   - Permissions-Policy
   - getCurrentPosition real (muestra coordenadas o error con code+message)

2. Botón "Probar getCurrentPosition" en la página de diagnóstico que
   ejecuta la llamada real y muestra el err.message literal del navegador
   (útil para detectar "Only secure origins are allowed", "User denied
   Geolocation", etc.)

3. race-distance-filter.tsx: ahora muestra el err.message real del
   navegador al final del mensaje de error, y detecta los strings típicos
   (secure origin, user denied) para dar mensajes más específicos.

4. Enlace "Diagnóstico avanzado" en el filtro de carreras, para que si
   falla el GPS, Manu (y cualquier usuario) pueda ir directo a /test-geo.

La página es segura de compartir — solo ejecuta checks en el navegador
del usuario, no envía nada a ningún servidor.
