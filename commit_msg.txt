refactor: quitar selector de ciudades, dejar solo GPS + manual

Manu dice que el GPS ya funciona perfectamente en su Chrome, así que el
selector de ciudades (14 botones de ciudades españolas) ya no aporta
valor y complica la UI.

Cambios en components/race-distance-filter.tsx:
- Eliminado el array SPANISH_CITIES (14 ciudades)
- Eliminada la función applyCity
- Eliminado el bloque JSX "Elige tu ciudad (1 click)"
- Eliminado el type "preset" del state coordsSource (queda "browser" | "manual" | null)
- Limpiada la lógica de sessionStorage para tratar sesiones viejas con
  source "ip" o "preset" como "browser" genérico
- Limpiados los textos de error/ayuda que mencionaban ciudades:
  * "Elige una ciudad o introduce coordenadas" → "Introduce las coordenadas manualmente"
  * "selector de ciudades o las coordenadas manuales" → "introducir las coordenadas manualmente"
  * "Elige una ciudad de la lista (1 click, funciona siempre)" → "Introduce las coordenadas manualmente (no requiere permiso)"
- Actualizado el comentario del header del componente

Métodos disponibles ahora:
1. GPS del navegador (botón "Activar GPS" con pre-prompt)
2. Coordenadas manuales (botón "Coordenadas manuales")

Bundle de /carreras: 11.7 kB → 11.2 kB
