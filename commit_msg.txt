fix: query systemListAll devolvía solo 6 campos (sin deep-extracted)

El check-bulk-status mostraba 0 con longDescription/altimetryData/raceFormats
pero en realidad se estaban guardando bien. La causa era que `systemListAll`
solo devolvía _id, name, slug, officialUrl, extractedAt, extractionConfidence.

Ahora devuelve también:
- extractedFromUrl
- longDescription
- altimetryData
- raceFormats
- aidStations
- priceTiers
- categories
- galleryUrls
- services
- organizer
- contactEmail

También añadí process.on('unhandledRejection'/'uncaughtException') en el
script para que un error aislado de la API no aborte el bulk completo.

Estado real del bulk (verificado tras el fix):
- 319/361 carreras con extractedAt (88%)
- 285 con longDescription (~80% del total)
- 234 con raceFormats (~65% del total)
- 0 con altimetryData (la IA no devolvió altimetría per-km; requiere
  un GPX/track subido, no se puede sacar solo de HTML)

Quedan 42 por procesar. El script es ahora robusto a unhandledRejection.
