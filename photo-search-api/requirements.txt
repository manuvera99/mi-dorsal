# Backend API
fastapi>=0.110
pydantic>=2.6

# Reconocimiento facial + OCR + procesado de imagen (findmyrace/)
numpy>=1.26
opencv-python-headless>=4.9
Pillow>=10.2
insightface>=0.7
onnxruntime>=1.16
easyocr>=1.7

# Descarga de álbumes/selfies
requests>=2.31
httpx>=0.27
# requests no descomprime Brotli (Content-Encoding: br) sin esto —
# confirmado real contra grupobrotons.com, falla en silencio sin ello.
brotli

# Config (dotenv es opcional — ver findmyrace/config.py, solo para dev local)
python-dotenv>=1.0
