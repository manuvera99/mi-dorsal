// =============================================================================
// DEPRECATED — re-exporta desde /components/json-ld para evitar duplicación.
// Este archivo existía en app/components/ antes de la migración SEO.
// Se mantiene como shim para que no rompa imports accidentales.
// =============================================================================

export {
  JsonLd,
  organizationJsonLd,
  websiteJsonLd,
  breadcrumbJsonLd,
  raceEventJsonLd,
  faqJsonLd,
} from "@/components/json-ld";
